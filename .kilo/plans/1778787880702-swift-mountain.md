Here's the honest gap analysis first, then the plan.

**What you currently cover vs Track 4:**

| Focus Area | Status |

|---|---|

| Analytics agents for NL querying | ✅ Done, excellent |

| AI-powered data pipelines & validation | ✅ Done (medallion ETL) |

| Anomaly detection | ✅ Done (KPI Monitoring page) |

| Forecasting | ❌ Missing |

| RAG over proprietary/multi-source data | ❌ Missing |

| Knowledge graph extraction from documents | ❌ Missing |

| Required tech: Gemini | ❌ Still on Grok |

You're covering 3 of 5 focus areas. A winning submission should hit at least 4. Here's the plan:

---

**The strategic insight: one new feature covers two missing areas at once.**

Add a **Document Intelligence page** — user uploads a PDF (hotel industry report, competitor analysis, market benchmarks). Gemini reads it, extracts entities and relationships as a knowledge graph, and the chat agent can then answer questions that span both your live database AND the document.

Example output a judge would see:

> *"Your Mumbai RevPAR of ₹7,240 is 12% above the industry benchmark of ₹6,450 per the Q2 2022 Hospitality Report you uploaded. However your cancellation rate (24%) exceeds the industry average (18%) — suggesting pricing power is strong but booking funnel needs attention."*

That single feature covers:

- **RAG over multi-source data** — document + database combined

- **Knowledge graph extraction from documents** — Gemini extracts entity-relationship structure from the PDF

And it uses Gemini **meaningfully** (multimodal PDF understanding), not just as a model swap.

---

**The complete winning plan — 5 changes, in priority order:**

**1. Gemini swap — 30 mins, non-negotiable**

In `utils/config.py`, one line:

```python

LLM_MODEL = "openrouter/google/gemini-2.5-flash"

```

Without this you may be ineligible. Do it first, test 3 queries, confirm function calling still works.

---

**2. Forecasting addition to KPI Monitoring — 2 hrs**

Add one section to the bottom of `03_KPI_Monitoring.py` — a `numpy.polyfit` linear projection on the existing trend data, shown as a dashed line extending 3 weeks ahead with a shaded confidence band. That covers "forecasting" completely. I'll write this as a drop-in code block.

---

**3. Document Intelligence page — 6–8 hrs, the differentiator**

New file: `frontend/pages/04_Document_Intelligence.py`

Three things happen on this page:

- User uploads a PDF (any hotel/hospitality report)

- Gemini reads it and returns structured JSON: extracted entities (hotels, cities, KPIs, benchmarks, trends) and their relationships

- Page shows: a knowledge graph visualization (networkx + plotly), a benchmark comparison table (industry numbers vs your database numbers), and a chat input where Gemini answers questions referencing both sources

This is the feature judges will remember. No other team will have it.

---

**4. Lobster Trap — 2 hrs, wins a second prize track**

3 lines in `agents/agents.py` wrapping every query with prompt inspection. Add a visible "🔒 Security verified" badge in the chat UI. Judges need to see it working — demo a blocked prompt injection attempt in your video.